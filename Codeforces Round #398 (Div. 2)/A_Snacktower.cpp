#pragma once
#include <iostream> 
#include <string>
#include <array>
#include <vector>
#include <algorithm>
#include <utility>
#include <set>
#include <cctype>
#include <cmath>
using std::cout, std::endl, std::cin, std::array, std::vector, std::string, std::pair, std::make_pair, std::set;


int main(){
    std::ios::sync_with_stdio(false);
    cin.tie(NULL);

    set<int, std::greater<int> > queue;
    int n; cin >> n;

    string ret = "";
    int b;

    return 0;
 }